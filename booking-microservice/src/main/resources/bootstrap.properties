#.properties of cloud bootstrap dependency recording the url where config-server is running
spring.cloud.config.uri=http://localhost:8888



##                                                           ...Transferred from .properties
#This is the "new" .properties because have priority on loading from Spring Cloud Server

#Sping Cloud Profile
spring.cloud.config.profile=dev

#DB Registration
spring.datasource.username=root
spring.datasource.password=1234